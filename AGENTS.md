# AGENTS.md

## 项目配置

- **权威规则文件**：无。产品范围与接口说明以 `README.md` 和 `docs/` 为准。
- **默认分支与合并方式**：默认分支为 `main`；合并方式未定义，未经用户明确要求不创建 PR 或合并。
- **提交与推送策略**：仅用户明确要求时提交或推送；Git 操作前保留并审阅工作区既有改动。
- **生产发布命令与权限边界**：未配置生产发布流程；本地不得直接发布，需用户明确授权并给出目标环境与回滚策略。
- **测试数据库/隔离方式**：测试 `DATABASE_URL` 必须指向本机隔离 PostgreSQL、临时 SQLite 文件或内存数据库；严禁连接生产库。
- **产品 hard gate 与发布验收入口**：未定义。当前最小工程验证为 `npm run typecheck`、`npm run build`、`npm test`；`new-api` Bridge 使用 `go test ./middleware ./service ./controller ./router`。
- **高风险数据规则**：不得直接写入生产 `new-api` 数据库；账户绑定、通知事件、工单和审计日志必须通过 Bot Repository 写入，敏感写操作需 scoped Bridge 和额外审查。

## 事实与判断

- 不默认认同用户或现有代码的判断。先检查问题中是否有错误前提、逻辑跳跃、信息缺失或未经证实的结论；发现后直接指出，不沿着错误前提推导。
- 明确区分已确认事实、合理推测、工程取舍和暂时无法验证的信息。事实、数字、日期、引用和案例应核对可追溯来源；无法确认时明确说明，不编造。
- 不同意某个判断时，给出可检验的原因、反例、风险或更合理解释；结论应能从证据和推理推出。

## 开工与范围

- 开工前阅读本文件、项目配置的权威规则、相关产品合同/任务账本；先运行 `git status --short --branch` 并查看当前 diff。
- 开工声明：唯一主目标、用户可观察结果、允许修改范围、明确不做、预计验证档位、停止条件。接手任务时先读取现状、已有证据和交接记录，从最后一个已验证断点继续。
- 只修改直接服务当前验收的文件。非阻塞扩项、顺手重构、新依赖、schema、入口和通用抽象默认不做。
- 发现工作树中的并行或用户改动时，不回滚、不覆盖、不暂存它们；提交时只添加本批明确拥有的文件。

## 工程实现取舍

- 已确认废弃且无外部合同的内部路径，不保留兼容层、fallback 或迁移壳；同批删除旧入口和旧实现。公开 API/URL、持久化数据、生产客户端或审计历史必须先证明消费者完成迁移。
- 选择完整满足已确认需求的最简单实现，避免为假设中的未来需求增加抽象、配置、间接层或扩展点。
- 按可工作的端到端薄层演进。最小版本也必须边界稳定、可部署、可继续演进，不能以明知将重写的临时方案替代现有可工作路径。
- 边界只能来自真实所有权、独立变化原因或多个当前调用方。单一调用方或“以后可能复用”不是新增 `service`、`controller`、`helper`、`registry`、`facade`、`hook` 或 wrapper 的理由。
- 新抽象必须写明当前真实调用方和被删除的旧路径；无新增用户能力的批次，模块数、入口数和净代码行应不增加，优先净减少。
- 同一能力的模式、模型、地区或内容差异进入声明式 spec 或小型差异函数；公共执行器、写入、错误处理、contract 和浏览器 harness 不得逐变体复制。
- 新增自有实现或依赖前，先核实项目现有依赖、版本、文档和实际能力；成熟且合适的库优先于重复实现。

## 测试与验证

- 测试代码与产品代码同样受质量约束。一个行为只保留一个权威 contract；变体用参数化矩阵覆盖；退役入口、不可达模块和已被更强合同完全覆盖的旧测试应同步删除。
- 新测试必须接入 focused command、CI 或明确的产品 gate。没有执行入口的测试不是有效覆盖。
- 测试严禁写生产数据或生产数据库；使用临时、隔离、可销毁的 fixture 与凭证。不得用真实外部副作用冒充本地验证。
- 每项验证都记录命令、输入/fixture、相关路径、结果和证据 SHA。无关提交不自动使证据失效；受影响代码、直接依赖、fixture 或配置变化才使其失效。
- focused contract 失败时，先修当前失败并重跑该子集；不得用重复执行全量 gate 制造进度。

### 验证分档

| 档位 | 适用影响 | 最小充分验证 |
| --- | --- | --- |
| `V0` | 文档、注释、非布局文案、同规格静态资源 | `git diff --check`；图片另验证存在、可解码和尺寸/格式 |
| `V1` | 单 surface 的视觉、排版、资源、局部响应式，无行为变化 | `V0` + 一个最能暴露问题的代表视口；只有改动涉及断点/换行时再加第二视口 |
| `V2` | 单一局部行为、状态、请求或读模型 | 最接近的 focused contract；可见交互再加一条代表浏览器路径 |
| `V3` | 共享组件、共享运行时或跨模块合同 | focused contract + 直接受影响的集成检查，按实际 surface 抽样 |
| `V4` | 发布、部署、安全/财务/隐私边界、跨产品核心或 hard gate | 对应产品总闸、发布检查或完整指定矩阵，仅在稳定批次运行一次 |

- 每个完成批次运行一次 `git diff --check`。只有改动影响编译/打包、依赖、构建配置、共享入口或发布产物时才运行 build。
- 可见交互或布局变化才做浏览器验收。不要机械执行桌面、平板、手机全矩阵；视口选择应由改动风险决定。
- 不得用单一 contract、单页 smoke 或旧截图外推整个产品完成。测试通过只证明其覆盖的行为。

## 高风险边界

- 数据迁移、鉴权/越权、支付/扣费、生产发布、隐私/安全、不可变账本、canonical 写入和外部副作用，必须在实现完成时立即运行 focused 正反向检查；不能拖到大批次末。
- 不执行破坏性命令、批量删除、覆盖数据、生产写入或对外发布，除非用户授权和项目流程都明确覆盖目标、范围与恢复策略。
- 人工试玩、无障碍真人审查、长期稳定性、线上平台审核和人工审美属于外部证据；自动化只能验证工具与边界，不得伪造已完成。

## Git、交付与并行协作

- 先遵循“项目配置”里的分支、提交、推送和发布策略。若未配置，默认不提交、不推送、不部署，直到用户明确授权。
- 提交、推送与测试是独立动作：不把测试设为 Git 操作的虚假前置条件，但必须如实报告未运行、失败和覆盖范围。
- 多 Agent 时，每人必须有互不重叠的文件所有权、可交付物和停止条件；主线程串行审阅 diff、处理冲突、接共享入口并统一验收。
- Agent 没有独立 commit 或可复用验证证据时，不因创建过分支就重复实现或强行合并。
- 最终报告说明：用户可观察结果、修改文件、验证档位与实际命令、沿用证据、明确未覆盖项和原因。不得把局部通过表述成整体发布就绪。
